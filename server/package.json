{
	"name": "cookbook-server",
	"version": "1.0.0",
	"main": "build/index.js",
	"type":"module",
	"scripts": {
		"develop": "concurrently 'npm run dev-build' 'npm run dev-run'",
		"dev-build": "npx tsc --watch",
		"dev-run": "node --watch ./build/index.js",
		"migrate": "flyway migrate",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "",
	"description": "",
	"devDependencies": {
		"@tsconfig/node20": "^20.1.4",
		"@types/express": "^5.0.5",
		"@types/node": "^24.10.1",
		"typescript": "^5.6.3"
	},
	"dependencies": {
		"express": "^5.1.0",
		"server-api": "file:../server-api"
	}
}
